import { arrayShuffle } from "../shuffle array/main";
export function changeImage1(): void {
    const imgContainer=document.querySelector(".image-container1")
    if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
    const img = document.createElement('img');
    img.src = arrayShuffle[Number(indiceId)].imagen;
    img.alt = `Image for item ${indiceId}`;
    imgContainer.innerHTML = "";
    imgContainer.appendChild(img);
    img.width = 150;
    img.height = 150;
    img.alt = 'imagenes de animales';
  }
    }
    
   
}
export function changeImage2(): void {
  const imgContainer=document.querySelector(".image-container2")
  if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
  const img = document.createElement('img');
  img.src = arrayShuffle[Number(indiceId)].imagen;
  img.alt = `Image for item ${indiceId}`;
  imgContainer.innerHTML = "";
  imgContainer.appendChild(img);
  img.width = 150;
  img.height = 150;
  img.alt = 'imagenes de animales';
}
  }
  
 
}
export function changeImage3(): void {
    const imgContainer=document.querySelector(".image-container3")
    if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
    const img = document.createElement('img');
    img.src = arrayShuffle[Number(indiceId)].imagen;
    img.alt = `Image for item ${indiceId}`;
    imgContainer.innerHTML = "";
    imgContainer.appendChild(img);
    img.width = 150;
    img.height = 150;
    img.alt = 'imagenes de animales';
  }
    }
    
   
}
export function changeImage4(): void {
  const imgContainer=document.querySelector(".image-container4")
  if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
  const img = document.createElement('img');
  img.src = arrayShuffle[Number(indiceId)].imagen;
  img.alt = `Image for item ${indiceId}`;
  imgContainer.innerHTML = "";
  imgContainer.appendChild(img);
  img.width = 150;
  img.height = 150;
  img.alt = 'imagenes de animales';
}
  }
  
 
}export function changeImage5(): void {
  const imgContainer=document.querySelector(".image-container5")
  if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
  const img = document.createElement('img');
  img.src = arrayShuffle[Number(indiceId)].imagen;
  img.alt = `Image for item ${indiceId}`;
  imgContainer.innerHTML = "";
  imgContainer.appendChild(img);
  img.width = 150;
  img.height = 150;
  img.alt = 'imagenes de animales';
}
  }
  
 
}export function changeImage6(): void {
  const imgContainer=document.querySelector(".image-container6")
  if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
  const img = document.createElement('img');
  img.src = arrayShuffle[Number(indiceId)].imagen;
  img.alt = `Image for item ${indiceId}`;
  imgContainer.innerHTML = "";
  imgContainer.appendChild(img);
  img.width = 150;
  img.height = 150;
  img.alt = 'imagenes de animales';
}
  }
  
 
}export function changeImage7(): void {
  const imgContainer=document.querySelector(".image-container7")
  if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
  const img = document.createElement('img');
  img.src = arrayShuffle[Number(indiceId)].imagen;
  img.alt = `Image for item ${indiceId}`;
  imgContainer.innerHTML = "";
  imgContainer.appendChild(img);
  img.width = 150;
  img.height = 150;
  img.alt = 'imagenes de animales';
}
  }
  
 
}

export function changeImage8(): void {
  const imgContainer=document.querySelector(".image-container8")
  if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
  const img = document.createElement('img');
  img.src = arrayShuffle[Number(indiceId)].imagen;
  img.alt = `Image for item ${indiceId}`;
  imgContainer.innerHTML = "";
  imgContainer.appendChild(img);
  img.width = 150;
  img.height = 150;
  img.alt = 'imagenes de animales';
}
  }
  
 
}export function changeImage9(): void {
  const imgContainer=document.querySelector(".image-container9")
  if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
  const img = document.createElement('img');
  img.src = arrayShuffle[Number(indiceId)].imagen;
  img.alt = `Image for item ${indiceId}`;
  imgContainer.innerHTML = "";
  imgContainer.appendChild(img);
  img.width = 150;
  img.height = 150;
  img.alt = 'imagenes de animales';
}
  }
  
 
}export function changeImage10(): void {
  const imgContainer=document.querySelector(".image-container10")
  if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
  const img = document.createElement('img');
  img.src = arrayShuffle[Number(indiceId)].imagen;
  img.alt = `Image for item ${indiceId}`;
  imgContainer.innerHTML = "";
  imgContainer.appendChild(img);
  img.width = 150;
  img.height = 150;
  img.alt = 'imagenes de animales';
}
  }
  
 
}export function changeImage11(): void {
  const imgContainer=document.querySelector(".image-container11")
  if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
  const img = document.createElement('img');
  img.src = arrayShuffle[Number(indiceId)].imagen;
  img.alt = `Image for item ${indiceId}`;
  imgContainer.innerHTML = "";
  imgContainer.appendChild(img);
  img.width = 150;
  img.height = 150;
  img.alt = 'imagenes de animales';
}
  }
  
 
}export function changeImage12(): void {
  const imgContainer=document.querySelector(".image-container12")
  if (imgContainer) {
const indiceId=imgContainer.getAttribute("data-indice-id");
if (indiceId && arrayShuffle[Number(indiceId)]) {
  const img = document.createElement('img');
  img.src = arrayShuffle[Number(indiceId)].imagen;
  img.alt = `Image for item ${indiceId}`;
  imgContainer.innerHTML = "";
  imgContainer.appendChild(img);
  img.width = 150;
  img.height = 150;
  img.alt = 'imagenes de animales';
}
  }
  
 
}
