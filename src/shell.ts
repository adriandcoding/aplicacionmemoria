import { crearTablero, handlerClickButton,clickStartButton } from "./ui";

document.addEventListener("DOMContentLoaded", () => {
    crearTablero();
    clickStartButton()
  handlerClickButton();
});
